import { Routes } from '@angular/router';
import { EstimationFormComponent } from './components/estimation-form.component';

export const ESTIMATION_ROUTES: Routes = [
  { path: '', component: EstimationFormComponent },
];
